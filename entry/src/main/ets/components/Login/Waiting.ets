import { AnimatorParams } from "@kit.MediaLibraryKit"

@Component
export struct Waiting{
  public onBack?:() => void = undefined;
  build(){
    RelativeContainer(){
      Text('hello~')
        .id('StuLogin.Text')
        .fontSize(18)
        .margin({
          top:'15%'
        })
        .alignRules({
          left:{anchor:"__container__",align:HorizontalAlign.Start},
          right:{anchor:"__container__",align:HorizontalAlign.End}
        })
        .textAlign(TextAlign.Center)
      TextInput({
        placeholder:'账号'
      })
        .id('StuLogin.Account')
        .backgroundColor(Color.White)
        .alignRules({
          top:{anchor:'StuLogin.Text',align:VerticalAlign.Bottom}
        })
        .margin({
          top:'15%',
          left:'5%',
          right:'5%'
        })
        .height(60)
        .border({
          color:Color.Gray,
          width:1,
          radius:30
        })
      TextInput({
        placeholder:'密码'
      })
        .id('StuLogin.Password')
        .backgroundColor(Color.White)
        .type(InputType.Password)
        .alignRules({
          top:{anchor:'StuLogin.Account',align:VerticalAlign.Bottom}
        })
        .margin({
          top:'5%',
          left:'5%',
          right:'5%'
        })
        .height(60)
        .border({
          color:Color.Gray,
          width:1,
          radius:30
        })
      Text('忘记密码？')
        .fontColor(Color.Red)
        .fontSize(12)
        .margin({
          top:'5%',
          left:'2%'
        })
        .alignRules({
          top:{anchor:'StuLogin.Password',align:VerticalAlign.Bottom},
          left:{anchor:'StuLogin.Password',align:HorizontalAlign.Start}
        })
      Button(){
        Text('欢迎登录')
          .font({
            size:18,
            weight:500
          })
          .fontColor(Color.White)
      }
        .backgroundColor('#249ae5')
        .width('50%')
        .height(60)
        .borderRadius(30)
        .margin({
          top:'15%'
        })
        .alignRules({
          top:{anchor:'StuLogin.Password',align:VerticalAlign.Bottom},
          middle:{anchor:'__container__',align:HorizontalAlign.Center}
        })
      Button(){
        Text('<')
      }
        .margin({
          left:'5%'
        })
        .backgroundColor('#99ddf3f1')
        .borderWidth(1)
        .borderColor(Color.Gray)
        .borderRadius(20)
        .width(40)
        .height(40)
        .onClick(()=>{
          this.onBack && this.onBack()
        })
    }

  }
}