import {CustomTab} from '../CustomComponents/CustomTab'
import display from '@ohos.display';

@Component
export struct Login{
  @Prop LoginType:number
  //@State LoginType:number=0

  customShadow:ShadowOptions={
    color:Color.Gray,
    radius:5,
    offsetX:5,
    offsetY:5,
    fill:false
  }

  build() {
    RelativeContainer(){
      RelativeContainer(){
        Column(){
          Blank()
            .height('100%')
            .width('100%')
            .backgroundColor(Color.White)
            .border({
              color:'#CCCCCC',
              radius:{
                topLeft:10,
                topRight:10
              },
              width:{
                top:2,
                left:2,
                right:0,
                bottom:0
              }
            })
        }
          .blur(5)
          .backgroundColor(Color.Transparent)
          .height(60)
          .borderRadius({
            topLeft:10,
            topRight:10
          })
          .alignRules({
            left:{anchor:'__container__',align:HorizontalAlign.Start},
            right:{anchor:'__container__',align:HorizontalAlign.End}
          })
        CustomTab({SelectIndex:this.LoginType,TabList:['学生','教师','超管']})
          .height(60)
          .width(240)
          //.backgroundColor(Color.Orange)
          .borderRadius({
            topLeft:10,
            topRight:10,
            bottomLeft:0,
            bottomRight:0
          })
      }
        .id('Tabs')
        .height(60)
        .width(240)
        .borderRadius({
          topLeft:10,
          topRight:10,
          bottomLeft:0,
          bottomRight:0
        })
        .alignRules({
          middle:{anchor:'__container__',align:HorizontalAlign.Center}
        })
        .shadow(this.customShadow)
      RelativeContainer(){
        Column(){
          Blank()
            .height('100%')
            .width('100%')
            .backgroundColor(Color.White)
            .border({
              color:'#CCCCCC',
              radius:8,
              width:{
                top:2,
                left:2,
                right:0,
                bottom:0
              }
            })
        }
          .blur(5)
          .backgroundColor(Color.Transparent)
          .height(235)
          .borderRadius(8)
          .alignRules({
            left:{anchor:'__container__',align:HorizontalAlign.Start},
            right:{anchor:'__container__',align:HorizontalAlign.End}
          })
        RelativeContainer(){
          Text('账号')
            .id('AccountText')
            .fontSize(18)
            .width(60)
            .margin({
              top:30,
              left:20
            })
          TextInput()
            .height(35)
            .alignRules({
              center:{anchor:'AccountText',align:VerticalAlign.Center},
              left:{anchor:'AccountText',align:HorizontalAlign.End},
              right:{anchor:'__container__',align:HorizontalAlign.End}
            })
            .margin({
              right:20
            })
          Text('密码')
            .id('PasswordText')
            .fontSize(18)
            .width(60)
            .margin({
              top:25,
              left:20
            })
            .alignRules({
              top:{anchor:'AccountText',align:VerticalAlign.Bottom}
            })
          TextInput()
            .type(InputType.Password)
            .height(35)
            .alignRules({
              center:{anchor:'PasswordText',align:VerticalAlign.Center},
              left:{anchor:'PasswordText',align:HorizontalAlign.End},
              right:{anchor:'__container__',align:HorizontalAlign.End}
            })
            .margin({
              right:20
            })
          Text('忘记密码？')
            .fontColor(Color.Red)
            .fontSize(12)
            .margin({
              top:20,
              left:20
            })
            .alignRules({
              top:{anchor:'PasswordText',align:VerticalAlign.Bottom}
            })
          Button(){
            Text('欢迎登录')
              .fontSize(28)
          }
          .backgroundColor('#77F0EC')
          .margin({
            top:60
          })
          .alignRules({
            top:{anchor:'PasswordText',align:VerticalAlign.Bottom},
            middle:{anchor:'__container__',align:HorizontalAlign.Center}
          })
          .height(54)
          .width(150)
          .borderRadius(8)
        }
          .height(235)
          .borderRadius(8)
      }
        .height(235)
        .borderRadius(8)
        .id('InputArea')
        .alignRules({
          top:{anchor:'Tabs',align:VerticalAlign.Bottom}
        })
        .shadow(this.customShadow)
    }
  }
}